{
  "name": "Python 3 + Claude Code",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "lts"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylint",
        "anthropic.claude-code"
      ]
    }
  },
  "postCreateCommand": "pip install -r requirements.txt || true && npm install -g @anthropic-ai/claude-code && npx --yes playwright install-deps && npx --yes playwright install chrome",
  "forwardPorts": [5173,8000,3000],
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/workspaces/${localWorkspaceFolderBasename}/.claude"
  }
}